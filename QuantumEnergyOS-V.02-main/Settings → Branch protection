name: Security Scan

on: [push, pull_request]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Python security scan
        run: |
          pip install pip-audit
          pip-audit

MAX_QUBITS = 32

if n_qubits > MAX_QUBITS:
    raise Exception("Invalid circuit size")
docker run --memory=4g --cpus=2 quantumenergyos

QuantumEnergyOS
│
├── core
│   ├── algorithms
│   ├── circuits
│   └── optimizers
│
├── infrastructure
│   ├── cloud_backends
│   ├── simulators
│   └── containers
│
├── security
│   ├── auth
│   ├── validation
│   └── sandbox
│
├── api
└── docs
hash = SHA256(circuit)
