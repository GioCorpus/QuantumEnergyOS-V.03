# QuantumEnergyOS V.02 — Browser Monitor Module
# Dependencias Python adicionales (las de server.py ya incluyen flask y flask-cors)
#
# Instalar:
#   pip install -r requirements_browser.txt
#
# Las únicas dependencias nuevas son para el módulo standalone.
# El módulo integrado en server.py solo necesita flask (ya instalado).

# Ya incluido en requirements.txt del servidor:
# flask>=2.3.0
# flask-cors>=4.0.0

# Para el script standalone browser_monitor_module.py (modo --standalone):
# (ninguna dependencia adicional — usa solo stdlib + flask)

# Para el script browser_cgroup_manager.py:
# (ninguna dependencia adicional — usa solo stdlib + subprocess)

# Opcional: para ejecutar el daemon Rust de forma automatizada
# psutil>=5.9.0     # alternativa Python-pura al daemon Rust (para desarrollo/debug)

# Opcional: para el QAOA con Qiskit real (desde qaoa_optimize.py del antigrav-bridge)
# qiskit>=1.0.0
# qiskit-aer>=0.14.0
# numpy>=1.24.0
