//! qeos-telemetryd — Userspace telemetry daemon
//!
//! This daemon reads frames from the DMA buffer (via mmap) and dispatches
//! them to configured consumers (database, ML pipeline, forecasting, etc.)

fn main() {
    eprintln!("qeos-telemetryd: not yet implemented");
    eprintln!("Run with --help for usage");
}
