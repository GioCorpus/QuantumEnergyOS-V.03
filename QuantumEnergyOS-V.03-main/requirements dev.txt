# ──────────────────────────────────────────────────────────────
# requirements-dev.txt — QuantumEnergyOS
# Dependencias SOLO para desarrollo y CI — NO para producción.
#
# Uso:
#   pip install -r requirements-pinned.txt -r requirements-dev.txt
# ──────────────────────────────────────────────────────────────

# Análisis estático y seguridad
bandit==1.8.3
semgrep==1.100.0
ruff==0.11.2
mypy==1.15.0

# Auditoría de dependencias
pip-audit==2.9.0
safety==3.5.0
cyclonedx-bom==4.6.0

# Testing
pytest==8.3.5
pytest-cov==6.1.0
pytest-asyncio==0.26.0
httpx==0.28.1              # cliente async para tests de FastAPI

# Jupyter (desarrollo local)
jupyter==1.1.0
ipykernel==6.29.5
ipywidgets==8.1.5
qsharp-widgets==1.11.0

# Notebooks
nbval==0.11.0              # pytest plugin para validar notebooks
